import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const slug = () => {
  return (
    <View>
      <Text>[slug]</Text>
    </View>
  )
}

export default slug

const styles = StyleSheet.create({})